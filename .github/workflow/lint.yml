name: Lint Checker

on:
    push:
        branches: [main, master]
    pull_request:
        branch: [main, master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install modules
      run: npm
    - name: Run ESLint
      run: eslint . --ext .ts
